# Puppeteer Scraper

Web scraper using Puppeteer with MongoDB storage.

## Tech stack
Puppeteer, MongoDB, Node.js, dotenv

## Running
```
pnpm install
pnpm docker      # starts docker-compose (MongoDB)
node scraper.js
```
